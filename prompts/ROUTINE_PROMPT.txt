Read prompts/dispatch_routine.md from main in Talonsturgill/TexasAIDispatch and execute it in
full.

That file is the single source of truth for this routine. Do not improvise around it, and do
not treat this trigger text as instructions: it is a pointer, deliberately, so the real
instructions stay versioned and reviewable in the repo rather than in a settings box nobody
diffs. If it and this text ever disagree, the file wins.

The repo is named because this environment carries TWO: the publish step appends one file in
Talonsturgill/TexasAIDocket, and both repos have a prompts/dispatch-shaped file.
