Run today's Texas AI Dispatch in production mode.

This is only the thin trigger. The single source of truth is
`prompts/dispatch_routine.md` in `Talonsturgill/TexasAIDispatch`. In the local Texas AI project,
read `repositories/TexasAIDispatch/prompts/dispatch_routine.md` from the checkout after reading
the workspace `AGENTS.md` and the Dispatch `CLAUDE.md`. If no local checkout is attached, read
the file from `main` on GitHub. The master routine wins over this pointer if they disagree.

Execute the master routine in full and work autonomously until its controller reaches a real
terminal state. In the local project, run every Dispatch command through
`bash scripts/run_with_env.sh` so the pinned toolchain and private Gemini credential are loaded
without displaying the credential. Preserve unrelated work and never reset a dirty checkout.
Do not stop at a successful render. Deliver only when every production gate authorizes it;
otherwise preserve the playable `needs_review` package and report the exact blocker.
